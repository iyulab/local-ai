# local-ai
.NET library for on-device AI inference: embeddings, reranking, text generation, chat, translation, OCR, image captioning, speech-to-text, text-to-speech, object detection, and segmentation—all without external API dependencies
